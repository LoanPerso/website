# Documentation System

## Goals
- Keep docs updated per change.
- Maintain a single source of truth.

## Process
- Update `docs/JOURNAL.md` and `docs/PROJECT_STATUS.md` for every change.
- Update `docs/PLAN.md` only after user validation.
